apiVersion: apps/v1
kind: Deployment
metadata:
  name: echo
  labels:
    app: echo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: echo
  template:
    metadata:
      labels:
        app: echo
    spec:
      containers:
        - name: echo
          image: ghcr.io/n-darshi888/local-k8s-echo:4de41fc
          args: ["-listen=:5678","-text=hello-from-gh-actions"]
          ports:
            - containerPort: 5678
      imagePullSecrets:
<<<<<<< HEAD
　　　　  - name: ghcr-secret
=======
        - name: ghcr-secret
>>>>>>> fix: set valid GHCR image for echo
---
apiVersion: v1
kind: Service
metadata:
  name: echo
spec:
  selector:
    app: echo
  ports:
    - name: http
      port: 80
      targetPort: 5678
